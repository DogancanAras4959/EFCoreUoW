@model UoW.DOMAIN.Models.Siparis
@using UoW.DOMAIN.Models
@using System;
@using System.Collections;
@using Microsoft.AspNetCore.Http;

@{
    ViewData["Title"] = "ResultOrder";
    Layout = "~/Views/Shared/_Layout.cshtml";

    //Fatura getBill = ViewBag.Fatura as Fatura;
    //Bayi getSeller = ViewBag.Seller as Bayi;

}

<div class="row justify-content-center mt-lg-6">
    <div class="col-xl-5 col-sm-8">
        <div class="card">
            <div class="card-body">
                @if (ViewBag.Ok == "1")
                {
                    <div class="text-center">
                        <div class="row justify-content-center">
                            <div class="col-lg-10">
                                <h4 class="mt-4 font-weight-semibold">SİPARİŞ ALINDI</h4>
                                <p class="text-muted mt-3">Ödeme işleminiz başarıyla tamamlandı! Alışverişe devam etmek için ana sayfaya dönüş yapabilirsiniz.</p>
                            </div>
                        </div>

                        <div class="row justify-content-center mt-5 mb-2">
                            <div class="col-sm-6 col-8">
                                <div>
                                    <img src="~/assets/images/alisverissonuc.jpg" alt="" class="img-fluid">
                                </div>
                            </div>
                        </div>
                    </div>
                }
                else
                {

                }
            </div>
        </div>
    </div>
</div>